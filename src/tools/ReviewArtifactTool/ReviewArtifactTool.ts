import { buildUnavailableTool } from '../../compat/unavailableTool.js'

export const ReviewArtifactTool = buildUnavailableTool(
  'ReviewArtifact',
  'Review artifact is unavailable in this leaked-src compatibility build.',
)
