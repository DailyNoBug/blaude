import { buildUnavailableTool } from '../../compat/unavailableTool.js'

export const PushNotificationTool = buildUnavailableTool(
  'PushNotification',
  'Push notifications are unavailable in this leaked-src compatibility build.',
)
