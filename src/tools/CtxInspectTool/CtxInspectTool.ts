import { buildUnavailableTool } from '../../compat/unavailableTool.js'

export const CtxInspectTool = buildUnavailableTool(
  'CtxInspect',
  'Context inspection is unavailable in this leaked-src compatibility build.',
)
