import { buildUnavailableTool } from '../../compat/unavailableTool.js'

export const MonitorTool = buildUnavailableTool(
  'Monitor',
  'Monitor execution is unavailable in this leaked-src compatibility build.',
)
