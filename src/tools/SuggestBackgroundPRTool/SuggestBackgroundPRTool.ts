import { buildUnavailableTool } from '../../compat/unavailableTool.js'

export const SuggestBackgroundPRTool = buildUnavailableTool(
  'SuggestBackgroundPR',
  'Background PR suggestions are unavailable in this leaked-src compatibility build.',
)
