import { buildUnavailableTool } from '../../compat/unavailableTool.js'

export const ListPeersTool = buildUnavailableTool(
  'ListPeers',
  'Peer listing is unavailable in this leaked-src compatibility build.',
)
