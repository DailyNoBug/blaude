import { buildUnavailableTool } from '../../compat/unavailableTool.js'

export const SubscribePRTool = buildUnavailableTool(
  'SubscribePR',
  'PR subscription is unavailable in this leaked-src compatibility build.',
)
