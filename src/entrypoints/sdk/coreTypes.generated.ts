// Placeholder for generated SDK types.
//
// The original leaked tree references a committed generated file here:
// `coreTypes.generated.js`, produced from `coreSchemas.ts`.
// Runtime code only needs this module to exist so that `coreTypes.ts` can
// re-export from it; the exported members are type-only in the original flow.
//
// We keep this shim intentionally empty to avoid changing leaked core logic.
export {}
