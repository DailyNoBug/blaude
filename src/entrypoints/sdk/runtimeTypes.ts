// Placeholder runtime SDK type module.
//
// The original project ships generated/committed runtime-facing type exports in
// this location. Current leaked runtime paths only need the module boundary to
// exist so `agentSdkTypes.ts` can re-export it.
export {}
